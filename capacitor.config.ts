import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arduinomobileapp.app',
  appName: 'arduino-mobile-app',
  webDir: 'out'
};

export default config;
